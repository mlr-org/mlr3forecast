% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{forecast}
\alias{forecast}
\title{Forecast from a trained forecast learner}
\usage{
forecast(object, task, h = 12L, newdata = NULL)
}
\arguments{
\item{object}{(\link[mlr3:Learner]{mlr3::Learner})\cr
A trained forecast learner.}

\item{task}{\link{TaskFcst}\cr
The task the learner was trained on.}

\item{h}{(\code{integer(1)})\cr
Forecast horizon, i.e. number of future steps per series. Default \code{12L}.}

\item{newdata}{(\code{\link[data.table:data.table]{data.table::data.table()}} | \code{NULL})\cr
Optional future values for exogenous features. Must contain the order column
(and key columns, if any); any additional columns overlay the auto-generated skeleton.}
}
\value{
A \link[mlr3:Prediction]{mlr3::Prediction}.
}
\description{
Extends \code{task} by \code{h} future rows and predicts with \code{object}. A thin wrapper around
\code{\link[=generate_newdata]{generate_newdata()}} and \code{object$predict_newdata()}.
}
\examples{
task = tsk("airpassengers")
flrn = as_learner_fcst(lrn("regr.rpart"), lags = 1:3)
flrn$train(task)
forecast(flrn, task, h = 12L)
}
