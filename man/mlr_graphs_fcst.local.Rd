% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_fcst_local.R
\name{mlr_graphs_fcst.local}
\alias{mlr_graphs_fcst.local}
\alias{pipeline_fcst_local}
\title{Create a Graph to Fit Local Per-Series Forecast Models}
\usage{
pipeline_fcst_local(graph, key = "key")
}
\arguments{
\item{graph}{(\link[mlr3pipelines:Graph]{Graph})\cr
Graph being wrapped between \code{\link[=mlr_pipeops_fcst.splitkey]{po("fcst.splitkey")}} and
\code{\link[=mlr_pipeops_fcst.unitekey]{po("fcst.unitekey")}}. The graph should return \code{NULL} during
training and a \link{PredictionFcst} during prediction.}

\item{key}{(\code{character(1)})\cr
Name of the rebuilt series-identity column in the united prediction's \code{extra} slot, default
\code{"key"}. Set it to the task's key column name to get predictions column-compatible with
global forecasters such as \link{RecursiveForecaster}.}
}
\value{
\link[mlr3pipelines:Graph]{Graph}
}
\description{
Create a new \link[mlr3pipelines:Graph]{Graph} that wraps \code{graph} between
\code{\link[=mlr_pipeops_fcst.splitkey]{po("fcst.splitkey")}} and
\code{\link[=mlr_pipeops_fcst.unitekey]{po("fcst.unitekey")}}, fitting one local model per series of a
keyed \link{TaskFcst} instead of one global model pooled across series.

All input arguments are cloned and have no references in common with the returned
\link[mlr3pipelines:Graph]{Graph}.
}
\examples{
\dontshow{if (requireNamespace("forecast", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(mlr3pipelines)
library(data.table)
dt = CJ(
  month = seq(as.Date("2024-01-01"), by = "month", length.out = 36L),
  id = factor(c("a", "b"))
)
dt[, value := rnorm(.N, mean = fifelse(id == "a", 10, 20))]
task = as_task_fcst(dt, target = "value", order = "month", key = "id", freq = "month")
flrn = as_learner(ppl("fcst.local", lrn("fcst.ets")))$train(task)
forecast(flrn, task, 12L)
\dontshow{\}) # examplesIf}
}
