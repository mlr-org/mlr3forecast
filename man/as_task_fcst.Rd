% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_task_fcst.R
\name{as_task_fcst}
\alias{as_task_fcst}
\alias{as_tasks_fcst}
\alias{as_tasks_fcst.default}
\alias{as_tasks_fcst.list}
\alias{as_task_fcst.TaskFcst}
\alias{as_task_fcst.DataBackend}
\alias{as_task_fcst.data.frame}
\alias{as_task_fcst.tsf}
\alias{as_task_fcst.ts}
\alias{as_task_fcst.zoo}
\alias{as_task_fcst.timeSeries}
\alias{as_task_fcst.tbl_ts}
\title{Convert to a Forecast Task}
\usage{
as_task_fcst(x, ...)

as_tasks_fcst(x, ...)

\method{as_tasks_fcst}{default}(x, ...)

\method{as_tasks_fcst}{list}(x, ...)

\method{as_task_fcst}{TaskFcst}(x, clone = FALSE, ...)

\method{as_task_fcst}{DataBackend}(
  x,
  target,
  order,
  key = character(),
  freq = NULL,
  period = NULL,
  id = deparse1(substitute(x)),
  label = NA_character_,
  ...
)

\method{as_task_fcst}{data.frame}(
  x,
  target,
  order,
  key = character(),
  freq = NULL,
  period = NULL,
  id = deparse1(substitute(x)),
  label = NA_character_,
  ...
)

\method{as_task_fcst}{tsf}(x, id = deparse1(substitute(x)), label = NA_character_, ...)

\method{as_task_fcst}{ts}(
  x,
  freq = NULL,
  period = NULL,
  id = deparse1(substitute(x)),
  label = NA_character_,
  ...
)

\method{as_task_fcst}{zoo}(
  x,
  freq = NULL,
  period = NULL,
  id = deparse1(substitute(x)),
  label = NA_character_,
  ...
)

\method{as_task_fcst}{timeSeries}(
  x,
  freq = NULL,
  period = NULL,
  id = deparse1(substitute(x)),
  label = NA_character_,
  ...
)

\method{as_task_fcst}{tbl_ts}(
  x,
  target,
  freq = NULL,
  period = NULL,
  id = deparse1(substitute(x)),
  label = NA_character_,
  ...
)
}
\arguments{
\item{x}{(any)\cr
Object to convert.}

\item{...}{(any)\cr
Additional arguments.}

\item{clone}{(\code{logical(1)})\cr
If \code{TRUE}, ensures that the returned object is not the same as the input \code{x}.}

\item{target}{(\code{character(1)})\cr
Name of the target column.}

\item{order}{(\code{character(1)})\cr
Name of the order column.}

\item{key}{(\code{character()})\cr
Names of the columns whose combination identifies a series.
Key columns must be character, integer, factor, or ordered columns.}

\item{freq}{(\code{character(1)} | \code{numeric(1)} | \code{NULL})\cr
Step of the time index, i.e. the spacing between two consecutive observations.
A \code{seq()}-compatible string for a \code{Date} or \code{POSIXct} order column, e.g.
\code{"1 month"}, \code{"day"}, \code{"3 months"}, \code{"1 hour"}, \code{"week"}.
A positive number for a numeric or integer order column.
If \code{NULL} (default), the step is inferred from the order column.
This is \emph{not} the seasonal period -- see \code{period}.}

\item{period}{(\code{character()} | \code{numeric()} | \code{NULL})\cr
Seasonal period(s) of the time series, in observations per cycle.
Either a positive number (or vector of them) such as \code{12}, or a cycle name resolved against
\code{freq} such as \code{"year"} or \code{"week"}. See \code{\link[=common_periods]{common_periods()}} for the cycles a frequency implies.
If \code{NULL} (default), the period is derived from \code{freq}: the shortest implied cycle spanning at
least four observations, e.g. \code{12} for monthly, \code{4} for quarterly, \code{7} for daily and \code{24} for
hourly data.
Sets the default for every learner, \code{PipeOp} and \code{Measure} that needs a seasonal period; each of
them accepts its own \code{period} hyperparameter that takes precedence.}

\item{id}{(\code{character(1)})\cr
Id for the new task.
Defaults to the (deparsed and substituted) name of the data argument.}

\item{label}{(\code{character(1)})\cr
Label for the new instance.}
}
\value{
\link{TaskFcst}.
}
\description{
Convert object to a \link{TaskFcst}.
This is a S3 generic. mlr3forecast ships with methods for the following objects:
\enumerate{
\item \link{TaskFcst}: ensure the identity
\item \code{\link[=data.frame]{data.frame()}} and \link[mlr3:DataBackend]{mlr3::DataBackend}: provides an alternative to the constructor of \link{TaskFcst}.
\item \code{ts}: from base R time series objects (univariate and multivariate).
\item \code{zoo} and \code{xts}: from zoo/xts time series objects.
\item \code{timeSeries}: from Rmetrics timeSeries objects.
\item \code{tsf}: from tsf format data.
\item \code{tbl_ts}: from tsibble objects.
}
}
\examples{
\dontshow{if (requireNamespace("tsbox", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(data.table)
airpassengers = tsbox::ts_dt(AirPassengers)
setnames(airpassengers, c("month", "passengers"))
as_task_fcst(airpassengers, target = "passengers", order = "month", freq = "month")
\dontshow{\}) # examplesIf}
}
