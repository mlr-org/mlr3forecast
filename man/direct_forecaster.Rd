% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/direct_forecaster.R
\name{direct_forecaster}
\alias{direct_forecaster}
\title{Create a Direct Forecast Learner}
\usage{
direct_forecaster(
  learner,
  lags,
  horizons,
  id = NULL,
  param_vals = list(),
  predict_type = NULL
)
}
\arguments{
\item{learner}{(\link[mlr3:Learner]{mlr3::Learner} | \link[mlr3pipelines:Graph]{mlr3pipelines::Graph} | \link[mlr3pipelines:PipeOp]{mlr3pipelines::PipeOp})\cr
A regression learner or a graph/PipeOp (without \link{PipeOpFcstLags}).}

\item{lags}{(\code{integer()})\cr
The base lag values. Exposed in \verb{$param_set} as \code{lags}, so it can be tuned via \link[mlr3tuning:AutoTuner]{mlr3tuning::AutoTuner}.}

\item{horizons}{(\code{integer()})\cr
Either a single integer \code{H} (expanded to \code{1:H}) or an integer vector of specific horizons. One model is trained
per horizon.}

\item{id}{(\code{character(1)} | \code{NULL})\cr
Identifier, default \code{NULL} (auto-generated from the learner id).}

\item{param_vals}{(named \code{list()})\cr
Hyperparameter values applied to every horizon model. Per-horizon hyperparameters are not currently supported.}

\item{predict_type}{(\code{character(1)} | \code{NULL})\cr
The predict type, default \code{NULL}.}
}
\value{
\link{DirectForecaster}.
}
\description{
Function to create a \link{DirectForecaster} object. This is the recommended way to construct a direct forecaster. It is a
thin wrapper around \code{DirectForecaster$new()}.

A direct forecaster trains a separate regression model per forecast horizon, so predictions never feed back into one
another (no error accumulation). For the recursive strategy (a single iterated model) see \code{\link[=recursive_forecaster]{recursive_forecaster()}}.
}
\examples{
library(mlr3pipelines)

task = tsk("airpassengers")
split = partition(task, ratio = 0.8)

# one model per horizon
flrn = direct_forecaster(lrn("regr.rpart"), lags = 1:3, horizons = length(split$test))
flrn$train(task, split$train)
flrn$predict(task, split$test)
}
