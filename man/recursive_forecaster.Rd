% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recursive_forecaster.R
\name{recursive_forecaster}
\alias{recursive_forecaster}
\title{Create a Recursive Forecast Learner}
\usage{
recursive_forecaster(
  learner,
  lags = NULL,
  id = NULL,
  param_vals = list(),
  predict_type = NULL,
  clone_graph = TRUE
)
}
\arguments{
\item{learner}{(\link[mlr3:Learner]{mlr3::Learner} | \link[mlr3pipelines:Graph]{mlr3pipelines::Graph} | \link[mlr3pipelines:PipeOp]{mlr3pipelines::PipeOp})\cr
A regression learner (when \code{lags} is provided) or a graph/PipeOp.}

\item{lags}{(\code{integer()} | \code{NULL})\cr
The lag values to use for creating lag features. If provided, \code{learner} is wrapped with
\code{po("fcst.lags", lags = lags)}. If \code{NULL}, \code{learner} must be a \link[mlr3pipelines:Graph]{mlr3pipelines::Graph} or \link[mlr3pipelines:PipeOp]{mlr3pipelines::PipeOp}.}

\item{id}{(\code{character(1)} | \code{NULL})\cr
Identifier, default \code{NULL} (auto-generated).}

\item{param_vals}{(named \code{list()})\cr
List of hyperparameter settings.}

\item{predict_type}{(\code{character(1)} | \code{NULL})\cr
The predict type, default \code{NULL}.}

\item{clone_graph}{(\code{logical(1)})\cr
Whether to clone the graph, default \code{TRUE}.}
}
\value{
\link{RecursiveForecaster}.
}
\description{
Function to create a \link{RecursiveForecaster} object. This is the recommended way to construct a recursive forecaster.
It is a thin wrapper around \code{RecursiveForecaster$new()}.

A recursive forecaster trains a single regression model and forecasts iteratively one step ahead, feeding each
prediction back as a lag/rolling feature for the next step. For the direct strategy (one model per horizon) see
\code{\link[=direct_forecaster]{direct_forecaster()}}.
}
\examples{
library(mlr3pipelines)

task = tsk("airpassengers")
split = partition(task, ratio = 0.8)

# simple: wrap a regression learner with lag features
flrn = recursive_forecaster(lrn("regr.rpart"), lags = 1:3)
flrn$train(task, split$train)
flrn$predict(task, split$test)

# graph: custom preprocessing pipeline
graph = po("fcst.lags", lags = 1:3) \%>>\% lrn("regr.rpart")
flrn = recursive_forecaster(graph)
}
