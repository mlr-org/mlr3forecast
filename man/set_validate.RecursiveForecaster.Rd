% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RecursiveForecaster.R
\name{set_validate.RecursiveForecaster}
\alias{set_validate.RecursiveForecaster}
\title{Configure Validation for a RecursiveForecaster}
\usage{
\method{set_validate}{RecursiveForecaster}(
  learner,
  validate,
  ids = NULL,
  args_all = list(),
  args = list(),
  ...
)
}
\arguments{
\item{learner}{(\link{RecursiveForecaster})\cr
The forecaster to configure.}

\item{validate}{(\code{numeric(1)} | \code{"predefined"} | \code{"test"} | \code{NULL})\cr
How to construct the internal validation data.}

\item{ids}{(\code{character()} | \code{NULL})\cr
The ids of the PipeOps for which to enable validation, forwarded to
\code{\link[mlr3pipelines:set_validate.GraphLearner]{mlr3pipelines::set_validate.GraphLearner()}}. Defaults to the base learner.}

\item{args_all}{(named \code{list()})\cr
Arguments passed to all \code{set_validate()} calls of the affected PipeOps.}

\item{args}{(named \code{list()} of named \code{list()}s)\cr
Arguments passed to the \code{set_validate()} calls of specific PipeOps, named by their ids.}

\item{...}{(any)\cr
Further arguments passed to \code{\link[mlr3pipelines:set_validate.GraphLearner]{mlr3pipelines::set_validate.GraphLearner()}}.}
}
\value{
\link{RecursiveForecaster}, invisibly.
}
\description{
Sets the \verb{$validate} field of the forecaster, which controls \emph{how} the validation data is
constructed (see \link[mlr3:Learner]{mlr3::Learner}), and configures the wrapped graph so its base learner uses it
(via \code{\link[mlr3pipelines:set_validate.GraphLearner]{mlr3pipelines::set_validate.GraphLearner()}}, the inner PipeOps receive \code{"predefined"}).
}
